# Backend environment example for Railway + local development
# Railway will inject DATABASE_URL automatically when you add a MySQL plugin.

# Optional: Railway provides DATABASE_URL in the form mysql://user:pass@host:port/dbname
DATABASE_URL=

# Alternatively set individual MYSQL_* vars for local development
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DB=church_planner_v2

SECRET_KEY=replace_with_a_strong_random_value
CORS_ORIGINS=http://127.0.0.1:5500,http://localhost:5500,http://localhost:3000
